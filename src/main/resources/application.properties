# Railway本番環境設定（緊急対応）
spring.profiles.active=prod

# データベース設定（緊急対応：直接設定）
spring.datasource.url=jdbc:mysql://root:AmbXzaNDVfeNfSASjvOppUrukQWvNFes@mysql.railway.internal:3306/railway?characterEncoding=UTF-8&useUnicode=true&serverTimezone=Asia/Tokyo
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=none

# デバッグ用ログ設定
logging.level.org.springframework.boot.autoconfigure.jdbc=DEBUG
logging.level.org.springframework.boot.context.config=DEBUG

# アプリケーション設定
spring.application.name=demo_wave
server.servlet.context-path=/demo_wave
server.port=${PORT:8080}

# エンコーディング設定
server.servlet.encoding.charset=UTF-8
server.servlet.encoding.enabled=true
server.servlet.encoding.force=true

# API設定
stripe.key.secret=${STRIPE_SECRET_KEY}
api.apikey=${API_KEY}

# Firebase設定
firebase.enabled=${FIREBASE_ENABLED:true}

# テンプレート設定
spring.thymeleaf.cache=true
spring.thymeleaf.encoding=UTF-8

# ログ設定
logging.level.org.springframework=INFO
logging.charset.console=UTF-8